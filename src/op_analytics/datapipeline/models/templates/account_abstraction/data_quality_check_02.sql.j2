WITH

uops AS (
  SELECT
    'user_ops' AS table_name
    , count(*) AS num_logs
  FROM account_abstraction__user_op_events
)

, traces AS (
  SELECT
    'user_ops' AS table_name
    , count(*) AS num_sender_subtraces
  FROM account_abstraction__user_op_sender_subtraces
  WHERE trace_row_number = 1
)

SELECT
  table_name
  , uops.num_logs
  , traces.num_sender_subtraces
FROM uops INNER JOIN traces USING (table_name)
WHERE uops.num_logs != traces.num_sender_subtraces
